{block title}New pad{/block}
{block header}New pad{/block}
{block content}
	{form newPadForm 'class' => 'pad'}
		<div class="alert-area" n:if="$form->hasErrors()">
			<div n:foreach="$form->errors as $error" class="alert alert-error">{$error}</div>
		</div>
	{label name /}
	{input name}
	{input submit}
	{/form}
{/block}
