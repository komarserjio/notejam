extends ../main

block content
  form.note(method='POST')
    label(for='name') Name
    input#name(type='text', name='name')
    +error('name', errors)
    label(for='text') Text
    textarea(name='text')
    +error('text', errors)
    label(for='pad_id') Pad
    select(name='pad_id')
      each pad in req.user.pads
        option(value=pad.id) #{pad.name}
    input(type='submit', value='Save')
