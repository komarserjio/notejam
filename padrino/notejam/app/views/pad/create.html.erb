<% content_for :page_title do %>New pad<% end %>

<% form_for :pad, url(:pad, :create) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= field_errors :name, @pad %>

  <%= f.submit "Create" %>
<% end %>

